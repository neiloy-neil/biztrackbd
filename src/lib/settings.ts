import { createClient } from './supabase/server'; import { unstable_cache } from 'next/cache'; import { createClient as createSupabaseClient } from '@supabase/supabase-js'; const getServiceRoleClient = () => { return createSupabaseClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!, { auth: { persistSession: false } }); }; export async function getPlatformSettingsCached() { return unstable_cache(async () => { const supabase = getServiceRoleClient(); const { data, error } = await supabase.from('platform_settings').select('*'); if (error) { console.error('Failed to fetch platform settings cache:', error); return null; } const settings: Record<string, any> = {}; data.forEach((row) => { settings[row.key] = row.value; }); return settings; }, ['platform_settings'], { revalidate: 300, tags: ['platform_settings'] })(); }
